// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  apiUrl:'https://lpfoauhrsl.execute-api.ap-southeast-1.amazonaws.com/dm/',
  orderServiceURL: "https://3xpr4y5qmc.execute-api.ap-southeast-1.amazonaws.com/dm",
  supplierItemRefundURL: "https://e5pic7irlg.execute-api.ap-southeast-1.amazonaws.com/dm",
  supplierAccessURL: "https://wba1lam6og.execute-api.ap-southeast-1.amazonaws.com/dm",
  serviceStations:"https://cy0caexk58.execute-api.ap-southeast-1.amazonaws.com/dm",
  profileService:"https://wba1lam6og.execute-api.ap-southeast-1.amazonaws.com/dm"
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
